import React from "react";
import "../Header/header.css"

const Header = () =>{
    return(
        <section className="testimonios_header">
            <span>FORO</span>
        </section>

    )
}

export default Header;